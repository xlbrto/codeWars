//Instructions required to turn a string into a number and I decided to use
//Number() method but I could've used parseInt() or parseFloat() to achieve the
//same result

//solution

const stringToNumber = function(str){
    return Number(str)
  }