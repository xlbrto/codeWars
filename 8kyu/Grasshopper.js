//Instruction requiered an "if else" conditional to get to the expected outcome
//case name equals owner "do x", otherwise "do y".

//solution

function greet (name, owner){
    if(name === owner){
      return 'Hello boss'
        }else
          return 'Hello guest'
  }