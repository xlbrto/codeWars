//I tried using Math.pow() but it wasn't the correct method
//so I figured I just had to multiply the number by itself to
//get the requested instruction.

//solution

function square(input){
    return input * input
  }
  
  square(3) 