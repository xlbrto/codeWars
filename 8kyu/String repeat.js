//had to figure out which method was going to give me the expected output
//in this case it was the "string".repeat() method x (n), which is how many times we're repeating
//the string

//solution

function repeatStr(n,s){
    return s.repeat(n)
  }